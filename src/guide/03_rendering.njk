---
title: Rendering
layout: "surround/guide_base.njk"
permalink: "guide/rendering/"
---

{% from "macros/render_controller.njk" import render_controller %}

{% markdown %}

# Rendering

Binder allows you to use JavaScript template literals wrapped in `{` and `}` within your custom elements.  
Anything with the `@render` attribute will be rendered as a template when `this.render()` is called.  

{% endmarkdown %}

{{ render_controller("partials/guide/rendering.njk", open=false) }}

{% markdown %}

<br />

There are two render modes, the first (shown above) is a basic replacement of variables within the controller.  
If you need more flexibility you can use `@render.eval` to use full JavaScript expressions in your template.
{% endmarkdown %}

{{ render_controller("partials/guide/rendering_maths.njk", open=false) }}

<fieldset class="admonition danger mt-15">
    <legend>CAREFUL</legend>
    When using <code>@render.eval</code> the JavaScript is actually executed, do not do this on untrusted input.
</fieldset>

{% markdown %}

<hr />

Next: [Arguments](/guide/arguments/)

{% endmarkdown %}
