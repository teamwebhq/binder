{% set title = "Examples" %}
{% extends "surround/base.njk" %}
{% from "macros/render_controller.njk" import render_controller %}

{% block content %}
    <div class="container">
        <h2>Examples</h2>

        <fieldset class="admonition information mt-15 mb-15">
            <legend>INFO</legend>
            Some of these examples put the logic in a JavaScript class and others use the base controller and have the logic in the HTML.<br />
            This is just to demonstrate the different ways you can use binder.<br />
            The on/off switch, for example, would probably be clearer with the logic in the HTML.
        </fieldset>

        <div class="example">
            {{ render_controller('partials/on_off_switch.njk') }}
        </div>

        <div class="example">
            {{ render_controller('partials/click_counter.njk') }}
        </div>

        <div class="example">
            {{ render_controller('partials/currency_picker.njk') }}
        </div>

        <div class="example">
            {{ render_controller('partials/magic_8_ball.njk') }}
        </div>

        <div class="example">
            {{ render_controller('partials/todo.njk') }}
        </div>

        <h2>Core Controllers</h2>

        <div class="example">
            {{ render_controller('partials/dynamic_frame.njk') }}
        </div>
    </div>

    <style>
        .example {
            margin-bottom: 60px;
        }
    </style>
{% endblock %}

